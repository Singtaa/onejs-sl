/** `onejs-sl/emit/unity`: the ShaderLab `.shader` a Unity editor generates for a program. */
export { emitShader, emitFragmentBody, uniformProperty } from "../hlsl"
export type { EmitOptions } from "../hlsl"
