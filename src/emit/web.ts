/** `onejs-sl/emit/web`: WGSL and GLSL ES 3.00 in OneJS's web frame, for a browser to compile. */
export { emitGLSL, emitWGSL, WEB_UNIFORM_SLOTS } from "../web"
export type { WebLanguage } from "../web"
